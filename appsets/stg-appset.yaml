apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: stg-apps
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/v01d42/argocd-sample.git
        revision: HEAD
        files:
          - path: "charts/*/values/stg-value.yaml"
  template:
    metadata:
      name: "monitor-{{ index .path.segments 1 }}"
    spec:
      project: default
      source:
        repoURL: https://github.com/v01d42/argocd-sample.git
        targetRevision: HEAD
        path: "{{index .path.segments 0 }}/{{index .path.segments 1}}"
        helm:
          valueFiles:
            - "values/stg-value.yaml"
      destination:
        server: https://kubernetes.default.svc
        namespace: stg
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
